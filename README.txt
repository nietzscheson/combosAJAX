1. Se genera código html con los CombosBox
2. Se crea el archivo ajax que genera el puente entre el html y el servidor.
3. El archivo modelo (donde están los datos de nuestra aplicación) envía los datos dependiendo del valor que se le haya pasado vía ajax.
4. Se procesan los datos en AJAX y se genera el html (los options) para pasarlos a la vista...
